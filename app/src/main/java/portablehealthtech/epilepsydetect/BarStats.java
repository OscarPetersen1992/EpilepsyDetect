/*package portablehealthtech.epilepsydetect;

import android.content.Context;
import android.database.Cursor;
import android.net.Uri;
import android.os.Bundle;
import android.support.v4.app.Fragment;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;

import com.github.mikephil.charting.charts.LineChart;
import com.github.mikephil.charting.utils.Utils;

public class BarStats extends Fragment {


    @Override
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);

    }

    @Override
    public View onCreateView(LayoutInflater inflater, ViewGroup container,
                             Bundle savedInstanceState) {
        /* Inflate the layout for this fragment
        return inflater.inflate(R.layout.fragment_bar_stats, container, false);

        ViewGroup rootView = (ViewGroup) inflater.inflate(R.layout.fragment_bar_stats,container,false);

        Context context = getActivity().getApplicationContext();

        DBHandler db = new DBHandler(context);

        Cursor cursor = db.getSeizure(2);

        cursor.moveToFirst();

        Utils.init(context);
       // LineChart mLineChart = (LineChart) findViewById(R.id.chartBar);



        return View;
    }



}
*/